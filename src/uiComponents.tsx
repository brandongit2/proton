import * as React from 'react';

export const Row = () => {
    return <div />;
};

export const Panel = (props: {state: any}) => {
    return <div />;
};

export const Button = (props: {children: string}) => {
    return <button type="button" className="ui button">{props.children}</button>;
};
