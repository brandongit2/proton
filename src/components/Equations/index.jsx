import PropTypes from 'prop-types';
import React from 'react';

export const Equations = ({properties}) => (
    <div className="equations">
        equations
    </div>
);

Equations.propTypes = {
    properties: PropTypes.object
};
